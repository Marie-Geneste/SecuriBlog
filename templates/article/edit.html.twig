{% extends 'base.html.twig' %}

{% block body %}
        <div class="create-article-container">
                <h1 class="create-article-title">Edition de l'article :  "{{article.title}}"</h1>
                <div class="create-article-form">
                    <div class="edit-article-form">
                        {{ include('article/_form.html.twig', {'button_label': 'Sauver'}) }}
                    </div>
                    <div class="delete-article-btn">
                        {{ include('article/_delete_form.html.twig') }}
                    </div>
                </div> 
        </div>
{% endblock %}
